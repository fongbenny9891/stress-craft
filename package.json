{
  "name": "stress-craft",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "docker-compose -f compose/docker-compose.yml up --build -d",
    "stop": "docker-compose -f compose/docker-compose.yml down",
    "restart": "docker-compose -f compose/docker-compose.yml down && docker-compose -f compose/docker-compose.yml up --build -d",
    "dev:frontend": "pnpm --filter frontend dev",
    "dev:node-backend": "pnpm --filter docker/node-backend dev",
    "dev:go-backend": "pnpm --filter docker/go-backend dev",
    "restart:all": "pnpm stop && rm -rf frontend/.next/cache && pnpm start && pnpm dev:frontend"
  },
  "devDependencies": {
    "dotenv": "^16.0.0"
  }
}